import React from "react";

const Navbar = () => {
  return (
    <div className="fixed inset-x-0 bg-black playwrite text-white h-[50px] flex justify-center items-center font-semibold">
      Array Visulizer
    </div>
  );
};

export default Navbar;
